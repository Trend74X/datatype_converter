name: datatype_converter
description: A Dart library for converting different type of datatypes to desired datatype output.
version: 0.0.1
homepage: https://github.com/Trend74X/datatype_converter/blob/master/README.md

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

# The following section is specific to Flutter.
flutter:
